# unifae-management

Painel Vue 3 (UNIFAE Management). Fluxo completo de setup e `npm run dev` na raiz está no **[README na raiz do monorepo](../README.md)**.

Nesta pasta, em geral:

- `npm run dev` — só o front (proxy para `http://localhost:3000`)
