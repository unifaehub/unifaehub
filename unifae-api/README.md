# unifae-api

API NestJS do projeto **UNIFAE**. Instalação, banco, seeds e execução conjunta com o painel estão documentados no **[README na raiz do monorepo](../README.md)**.

Comandos úteis nesta pasta:

- `npm run start:dev` — desenvolvimento
- `npm run seed` / `npm run seed:full` — dados de exemplo
